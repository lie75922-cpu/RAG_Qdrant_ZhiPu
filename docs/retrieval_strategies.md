# Retrieval Strategies

`dense` searches vector stores. `bm25` scores lexical overlap. `hybrid` combines dense and BM25 results with Reciprocal Rank Fusion.

V2 adds `query_rewrite`, which runs multiple rewritten query variants and fuses the results with RRF.

