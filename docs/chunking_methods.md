# Chunking Methods

`recursive` performs overlapping character chunks. `token` performs overlapping whitespace-token chunks.

V2 adds `semantic`, a lightweight sentence grouping strategy, and `parent_child`, which indexes smaller child chunks while preserving parent context in metadata.

